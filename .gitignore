__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
.ipynb_checkpoints/
*.egg-info/
.pytest_cache/

# Large regenerable artefacts: corpus cache, embeddings, FAISS indexes, LoRA weights.
# Rebuild with scripts/prepare_data.py && scripts/build_index.py && scripts/train.py
artifacts/
data/cache/
*.faiss
*.npy

# MLflow local store (regenerated by rerunning the experiment scripts)
mlruns/
mlflow.db

# Raw stdout from experiment runs. The parsed results in results/**/*.json are
# committed; these transcripts are not.
results/*.log

# Committed on purpose, because they are the evidence behind the README:
#   results/**/*.json, results/**/*.md, results/figures/*.png
